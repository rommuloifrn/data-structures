# Heap Tree (Fila de Prioridade)

Uma árvore heap é uma árvore binária que retorna o maior (no caso de uma max heap) ou menor (min heap) elemento em tempo O(1).

Índice:

- Inserção
- Operação Upheap
- Remoção
- Operação Downheap
- Heap Sort
- Vetor
